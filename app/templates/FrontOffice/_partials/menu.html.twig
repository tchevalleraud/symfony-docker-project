{% set activeRoute = app.request.attributes.get('_route') %}
<ul class="navbar-nav me-auto mb-2 mb-lg-0">
    <li class="nav-item">
        <a class="nav-link {{ 'frontoffice.dashboard.' in activeRoute ? 'active' }}" href="{{ path('frontoffice.dashboard.index') }}">Dashboard</a>
    </li>
</ul>